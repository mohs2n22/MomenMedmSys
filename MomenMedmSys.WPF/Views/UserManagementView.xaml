<UserControl x:Class="MomenMedmSys.WPF.Views.UserManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="User Management" FontSize="28" FontWeight="Bold" Foreground="#1E293B"/>
            <TextBlock Text="Manage system users, roles, and access permissions" FontSize="14" Foreground="#64748B"/>
        </StackPanel>

        <!-- Toolbar -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,16">
            <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" Width="280" Padding="8,6" FontSize="14"
                     BorderBrush="#E2E8F0" BorderThickness="1" VerticalContentAlignment="Center">
                <TextBox.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="6"/>
                    </Style>
                </TextBox.Resources>
            </TextBox>
            <TextBlock Text="&#x1F50D;" FontSize="16" Margin="-28,0,0,0" VerticalAlignment="Center" Foreground="#94A3B8" IsHitTestVisible="False"/>

            <TextBlock Text="Role:" VerticalAlignment="Center" Margin="16,0,8,0" Foreground="#64748B"/>
            <ComboBox Text="{Binding FilterRole, UpdateSourceTrigger=PropertyChanged}" Width="160" Padding="8,6" FontSize="14"
                      BorderBrush="#E2E8F0" BorderThickness="1" Margin="0,0,16,0">
                <ComboBoxItem Content="All"/>
                <ComboBoxItem Content="Admin"/>
                <ComboBoxItem Content="Manager"/>
                <ComboBoxItem Content="Technician"/>
                <ComboBoxItem Content="Viewer"/>
            </ComboBox>

            <Button Content="&#x2795; Add" Command="{Binding AddUserCommand}" Style="{StaticResource PrimaryBtn}" Width="90" Margin="0,0,8,0"/>
            <Button Content="&#x270F;&#xFE0F; Edit" Command="{Binding EditUserCommand}" Style="{StaticResource SecondaryBtn}" Width="90" Margin="0,0,8,0"/>
            <Button Content="&#x1F5D1;&#xFE0F; Delete" Command="{Binding DeleteUserCommand}" Style="{StaticResource SecondaryBtn}" Width="90" Margin="0,0,8,0"/>
            <Button Content="&#x1F512; Lock" Command="{Binding ToggleUserLockCommand}" Style="{StaticResource SecondaryBtn}" Width="90" Margin="0,0,8,0"/>
            <Button Content="&#x1F511; Reset Pwd" Command="{Binding ResetUserPasswordCommand}" Style="{StaticResource SecondaryBtn}" Width="110" Margin="0,0,8,0"/>
            <Button Content="&#x1F504; Refresh" Command="{Binding LoadUsersCommand}" Style="{StaticResource SecondaryBtn}" Width="100"/>
        </StackPanel>

        <!-- DataGrid -->
        <DataGrid Grid.Row="2" ItemsSource="{Binding FilteredUsers}" SelectedItem="{Binding SelectedUser}"
                  Style="{StaticResource ModernDataGrid}" IsReadOnly="True" AutoGenerateColumns="False"
                  SelectionMode="Single" CanUserSortColumns="True" CanUserResizeColumns="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Username" Binding="{Binding Username}" Width="140"/>
                <DataGridTextColumn Header="Full Name" Binding="{Binding FullName}" Width="180"/>
                <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="200"/>
                <DataGridTemplateColumn Header="Role" Width="120">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Border CornerRadius="4" Padding="6,2" HorizontalAlignment="Center">
                                <TextBlock Text="{Binding Role}" FontSize="11" FontWeight="SemiBold" HorizontalAlignment="Center">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Role}" Value="Admin">
                                                    <Setter Property="Foreground" Value="#7C3AED"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Role}" Value="Manager">
                                                    <Setter Property="Foreground" Value="#1D4ED8"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Role}" Value="Technician">
                                                    <Setter Property="Foreground" Value="#0F766E"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Role}" Value="Viewer">
                                                    <Setter Property="Foreground" Value="#6B7280"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Role}" Value="Admin">
                                                <Setter Property="Background" Value="#EDE9FE"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Role}" Value="Manager">
                                                <Setter Property="Background" Value="#DBEAFE"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Role}" Value="Technician">
                                                <Setter Property="Background" Value="#CCFBF1"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Role}" Value="Viewer">
                                                <Setter Property="Background" Value="#F3F4F6"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                            </Border>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="Last Login" Binding="{Binding LastLoginDate, StringFormat=yyyy-MM-dd HH:mm}" Width="150"/>
                <DataGridTextColumn Header="Failed Attempts" Binding="{Binding FailedLoginAttempts}" Width="110"/>
                <DataGridTemplateColumn Header="Status" Width="100">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Border CornerRadius="4" Padding="4,2" HorizontalAlignment="Center">
                                <TextBlock FontSize="10" FontWeight="SemiBold" HorizontalAlignment="Center">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Text" Value="Active"/>
                                            <Setter Property="Foreground" Value="#166534"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsActive}" Value="False">
                                                    <Setter Property="Text" Value="Inactive"/>
                                                    <Setter Property="Foreground" Value="#991B1B"/>
                                                </DataTrigger>
                                                <MultiDataTrigger>
                                                    <MultiDataTrigger.Conditions>
                                                        <Condition Binding="{Binding IsActive}" Value="True"/>
                                                        <Condition Binding="{Binding IsLocked}" Value="True"/>
                                                    </MultiDataTrigger.Conditions>
                                                    <Setter Property="Text" Value="Locked"/>
                                                    <Setter Property="Foreground" Value="#DC2626"/>
                                                </MultiDataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Setter Property="Background" Value="#DCFCE7"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsActive}" Value="False">
                                                <Setter Property="Background" Value="#FEE2E2"/>
                                            </DataTrigger>
                                            <MultiDataTrigger>
                                                <MultiDataTrigger.Conditions>
                                                    <Condition Binding="{Binding IsActive}" Value="True"/>
                                                    <Condition Binding="{Binding IsLocked}" Value="True"/>
                                                </MultiDataTrigger.Conditions>
                                                <Setter Property="Background" Value="#FEE2E2"/>
                                            </MultiDataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                            </Border>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Status Bar -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,12,0,0">
            <TextBlock Text="{Binding StatusMessage}" Foreground="#64748B" FontSize="12"/>
            <TextBlock Text=" | " Foreground="#CBD5E1" FontSize="12"/>
            <TextBlock Foreground="#64748B" FontSize="12">
                <TextBlock.Text>
                    <MultiBinding StringFormat="Showing {0} of {1} users">
                        <Binding Path="FilteredUsers.Count"/>
                        <Binding Path="Users.Count"/>
                    </MultiBinding>
                </TextBlock.Text>
            </TextBlock>
        </StackPanel>
    </Grid>
</UserControl>

