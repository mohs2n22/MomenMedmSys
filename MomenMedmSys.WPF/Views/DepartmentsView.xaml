<UserControl x:Class="MomenMedmSys.WPF.Views.DepartmentsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="Departments" FontSize="28" FontWeight="Bold" Foreground="#1E293B"/>
            <TextBlock Text="Manage hospital departments and locations" FontSize="14" Foreground="#64748B"/>
        </StackPanel>

        <!-- Toolbar -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,16">
            <Button Content="➕ Add" Command="{Binding AddDepartmentCommand}" Style="{StaticResource PrimaryBtn}" Width="90" Margin="0,0,8,0"/>
            <Button Content="✏️ Edit" Command="{Binding EditDepartmentCommand}" Style="{StaticResource SecondaryBtn}" Width="90" Margin="0,0,8,0"/>
            <Button Content="🗑️ Delete" Command="{Binding DeleteDepartmentCommand}" Style="{StaticResource SecondaryBtn}" Width="90" Margin="0,0,8,0"/>
            <Button Content="🔄 Refresh" Command="{Binding RefreshCommand}" Style="{StaticResource SecondaryBtn}" Width="100"/>
        </StackPanel>

        <!-- DataGrid -->
        <DataGrid Grid.Row="2" ItemsSource="{Binding Departments}" 
                  Style="{StaticResource ModernDataGrid}" IsReadOnly="True" AutoGenerateColumns="False"
                  SelectionMode="Single" CanUserSortColumns="True" CanUserResizeColumns="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Code" Binding="{Binding Code}" Width="100"/>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="200"/>
                <DataGridTextColumn Header="Location" Binding="{Binding Location}" Width="150"/>
                <DataGridTextColumn Header="Floor" Binding="{Binding Floor}" Width="80"/>
                <DataGridTextColumn Header="Building" Binding="{Binding Building}" Width="120"/>
                <DataGridTextColumn Header="Head of Department" Binding="{Binding HeadOfDepartment}" Width="150"/>
                <DataGridTextColumn Header="Contact" Binding="{Binding ContactPhone}" Width="120"/>
                <DataGridTemplateColumn Header="Active" Width="80">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding IsActive}" HorizontalAlignment="Center" VerticalAlignment="Center">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsActive}" Value="True">
                                                <Setter Property="Foreground" Value="#166534"/>
                                                <Setter Property="Text" Value="✓"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding IsActive}" Value="False">
                                                <Setter Property="Foreground" Value="#991B1B"/>
                                                <Setter Property="Text" Value="✗"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Status Bar -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,12,0,0">
            <TextBlock Text="{Binding StatusMessage}" Foreground="#64748B" FontSize="12"/>
            <TextBlock Text=" | " Foreground="#CBD5E1" FontSize="12"/>
            <TextBlock Foreground="#64748B" FontSize="12">
                <TextBlock.Text>
                    <MultiBinding StringFormat="Total: {0} | Active: {1}">
                        <Binding Path="TotalCount"/>
                        <Binding Path="ActiveCount"/>
                    </MultiBinding>
                </TextBlock.Text>
            </TextBlock>
        </StackPanel>
    </Grid>
</UserControl>

