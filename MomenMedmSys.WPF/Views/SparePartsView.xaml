<UserControl x:Class="MomenMedmSys.WPF.Views.SparePartsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="📦 Spare Parts" FontSize="28" FontWeight="Bold" Foreground="{StaticResource TextPrimary}"/>
            <TextBlock Text="Manage spare parts inventory" FontSize="14" Foreground="{StaticResource TextSecondary}"/>
        </StackPanel>
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,16">
            <Border Style="{StaticResource StatCardTeal}" Margin="0,0,10,0" MinWidth="120">
                <StackPanel Orientation="Horizontal"><TextBlock Text="💰 Value: " Foreground="{StaticResource TextOnPrimary}" FontSize="12"/><TextBlock Text="{Binding TotalInventoryValue, StringFormat=${0:N2}}" Foreground="{StaticResource TextOnPrimary}" FontSize="14" FontWeight="Bold"/></StackPanel>
            </Border>
            <Border Style="{StaticResource StatCardDanger}" Margin="0,0,10,0" MinWidth="100">
                <StackPanel Orientation="Horizontal"><TextBlock Text="⚠️ Low Stock: " Foreground="{StaticResource TextOnPrimary}" FontSize="12"/><TextBlock Text="{Binding LowStockCount}" Foreground="{StaticResource TextOnPrimary}" FontSize="14" FontWeight="Bold"/></StackPanel>
            </Border>
            <Button Content="➕ Add" Command="{Binding AddPartCommand}" Style="{StaticResource PrimaryButtonStyle}" Width="80" Margin="8,0,8,0"/>
            <Button Content="✏️ Edit" Command="{Binding EditPartCommand}" Style="{StaticResource SecondaryButtonStyle}" Width="80" Margin="0,0,8,0"/>
            <Button Content="🗑️ Delete" Command="{Binding DeletePartCommand}" Style="{StaticResource SecondaryButtonStyle}" Width="80" Margin="0,0,8,0"/>
            <Button Content="🔄 Refresh" Command="{Binding LoadPartsCommand}" Style="{StaticResource SecondaryButtonStyle}" Width="100"/>
        </StackPanel>
        <DataGrid Grid.Row="2" ItemsSource="{Binding Parts}" SelectedItem="{Binding SelectedPart}"
                  Style="{StaticResource DataGridStyle}" IsReadOnly="True" AutoGenerateColumns="False"
                  SelectionMode="Single" CanUserSortColumns="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Part No." Binding="{Binding PartNumber}" Width="90"/>
                <DataGridTextColumn Header="Name" Binding="{Binding PartName}" Width="180"/>
                <DataGridTextColumn Header="Category" Binding="{Binding Category}" Width="120"/>
                <DataGridTextColumn Header="Device" Binding="{Binding Device.DeviceName}" Width="140"/>
                <DataGridTemplateColumn Header="Stock" Width="70">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding CurrentStock}" FontWeight="Bold" HorizontalAlignment="Center">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsLowStock}" Value="True">
                                                <Setter Property="Foreground" Value="{StaticResource DangerColor}"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="Min" Binding="{Binding MinimumStock}" Width="60"/>
                <DataGridTextColumn Header="Max" Binding="{Binding MaximumStock}" Width="60"/>
                <DataGridTextColumn Header="Unit Cost" Binding="{Binding UnitCost, StringFormat=C2}" Width="90"/>
                <DataGridTextColumn Header="Supplier" Binding="{Binding SupplierName}" Width="120"/>
                <DataGridTextColumn Header="Critical" Binding="{Binding IsCritical}" Width="70"/>
                <DataGridTextColumn Header="Total Used" Binding="{Binding TotalUsageCount}" Width="80"/>
            </DataGrid.Columns>
        </DataGrid>
        <TextBlock Grid.Row="3" Text="{Binding StatusMessage}" Foreground="{StaticResource TextSecondary}" FontSize="12" Margin="0,8,0,0"/>
    </Grid>
</UserControl>
