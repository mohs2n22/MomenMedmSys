<UserControl x:Class="MomenMedmSys.WPF.Views.AdminControlPanelView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="380"/>
        </Grid.ColumnDefinitions>

        <!-- Left Panel: Staff Lists -->
        <Grid Grid.Column="0" Margin="0,0,16,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <StackPanel Grid.Row="0" Margin="0,0,0,16">
                <TextBlock Text="⚙️ Admin Control Panel" FontSize="28" FontWeight="Bold" Foreground="{StaticResource TextPrimary}"/>
                <TextBlock Text="Manage staff accounts, roles, and permissions" FontSize="14" Foreground="{StaticResource TextSecondary}"/>
            </StackPanel>

            <!-- Stats Bar -->
            <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,16">
                <Border Style="{StaticResource StatCardPrimary}" Margin="0,0,10,0" MinWidth="100">
                    <StackPanel Orientation="Horizontal"><TextBlock Text="👥 Total: " Foreground="{StaticResource TextOnPrimary}" FontSize="12"/><TextBlock Text="{Binding TotalStaff}" Foreground="{StaticResource TextOnPrimary}" FontSize="14" FontWeight="Bold"/></StackPanel>
                </Border>
                <Border Style="{StaticResource StatCardSuccess}" Margin="0,0,10,0" MinWidth="100">
                    <StackPanel Orientation="Horizontal"><TextBlock Text="✅ Active: " Foreground="{StaticResource TextOnPrimary}" FontSize="12"/><TextBlock Text="{Binding ActiveAccounts}" Foreground="{StaticResource TextOnPrimary}" FontSize="14" FontWeight="Bold"/></StackPanel>
                </Border>
                <Border Style="{StaticResource StatCardDanger}" Margin="0,0,10,0" MinWidth="100">
                    <StackPanel Orientation="Horizontal"><TextBlock Text="🔒 Locked: " Foreground="{StaticResource TextOnPrimary}" FontSize="12"/><TextBlock Text="{Binding LockedAccounts}" Foreground="{StaticResource TextOnPrimary}" FontSize="14" FontWeight="Bold"/></StackPanel>
                </Border>
                <Button Content="🔄 Refresh" Command="{Binding LoadAllCommand}" Style="{StaticResource PrimaryButtonStyle}" Width="100" Margin="16,0,8,0"/>
            </StackPanel>

            <!-- Tabbed Lists -->
            <TabControl Grid.Row="2" SelectedIndex="{Binding ActiveTabIndex}">
                <TabItem Header="👥 All Staff">
                    <DataGrid ItemsSource="{Binding AllStaff}" SelectedItem="{Binding SelectedStaff}"
                              Style="{StaticResource DataGridStyle}" IsReadOnly="True" AutoGenerateColumns="False"
                              SelectionMode="Single" CanUserSortColumns="True">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Emp ID" Binding="{Binding EmployeeId}" Width="90"/>
                            <DataGridTextColumn Header="Name" Binding="{Binding FullName}" Width="150"/>
                            <DataGridTextColumn Header="Username" Binding="{Binding Username}" Width="120"/>
                            <DataGridTextColumn Header="Role" Binding="{Binding Role}" Width="130"/>
                            <DataGridTextColumn Header="Department" Binding="{Binding Department}" Width="110"/>
                            <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="180"/>
                            <DataGridTemplateColumn Header="Status" Width="80">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Border CornerRadius="4" Padding="4,2" HorizontalAlignment="Center">
                                            <TextBlock Text="{Binding IsLocked, StringFormat={}{0}}" FontSize="10" FontWeight="SemiBold" HorizontalAlignment="Center"/>
                                            <Border.Style>
                                                <Style TargetType="Border">
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsLocked}" Value="True">
                                                            <Setter Property="Background" Value="{StaticResource DangerBgColor}"/>
                                                            <Setter Property="TextElement.Foreground" Value="{StaticResource TextOnDanger}"/>
                                                        </DataTrigger>
                                                        <DataTrigger Binding="{Binding IsLocked}" Value="False">
                                                            <Setter Property="Background" Value="{StaticResource SuccessBgColor}"/>
                                                            <Setter Property="TextElement.Foreground" Value="{StaticResource TextOnSuccess}"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Border.Style>
                                        </Border>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </TabItem>
                <TabItem Header="🔑 Administrators">
                    <DataGrid ItemsSource="{Binding Administrators}" SelectedItem="{Binding SelectedStaff}"
                              Style="{StaticResource DataGridStyle}" IsReadOnly="True" AutoGenerateColumns="False">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Name" Binding="{Binding FullName}" Width="180"/>
                            <DataGridTextColumn Header="Username" Binding="{Binding Username}" Width="140"/>
                            <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="200"/>
                            <DataGridTextColumn Header="Admin Panel" Binding="{Binding CanAccessAdminPanel}" Width="100"/>
                            <DataGridTextColumn Header="Manage Devices" Binding="{Binding CanManageDevices}" Width="120"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </TabItem>
                <TabItem Header="🔧 Hardware Techs">
                    <DataGrid ItemsSource="{Binding HardwareTechnicians}" SelectedItem="{Binding SelectedStaff}"
                              Style="{StaticResource DataGridStyle}" IsReadOnly="True" AutoGenerateColumns="False">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Name" Binding="{Binding FullName}" Width="180"/>
                            <DataGridTextColumn Header="Username" Binding="{Binding Username}" Width="140"/>
                            <DataGridTextColumn Header="Specialization" Binding="{Binding Specialization}" Width="180"/>
                            <DataGridTextColumn Header="Department" Binding="{Binding Department}" Width="120"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </TabItem>
                <TabItem Header="📝 Report Writers">
                    <DataGrid ItemsSource="{Binding ReportWriters}" SelectedItem="{Binding SelectedStaff}"
                              Style="{StaticResource DataGridStyle}" IsReadOnly="True" AutoGenerateColumns="False">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Name" Binding="{Binding FullName}" Width="180"/>
                            <DataGridTextColumn Header="Username" Binding="{Binding Username}" Width="140"/>
                            <DataGridTextColumn Header="Department" Binding="{Binding Department}" Width="140"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </TabItem>
                <TabItem Header="🩺 Physicians">
                    <DataGrid ItemsSource="{Binding Physicians}" SelectedItem="{Binding SelectedStaff}"
                              Style="{StaticResource DataGridStyle}" IsReadOnly="True" AutoGenerateColumns="False">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Name" Binding="{Binding FullName}" Width="180"/>
                            <DataGridTextColumn Header="Username" Binding="{Binding Username}" Width="140"/>
                            <DataGridTextColumn Header="Specialization" Binding="{Binding Specialization}" Width="180"/>
                            <DataGridTextColumn Header="Department" Binding="{Binding Department}" Width="120"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </TabItem>
                <TabItem Header="💊 Nurses">
                    <DataGrid ItemsSource="{Binding Nurses}" SelectedItem="{Binding SelectedStaff}"
                              Style="{StaticResource DataGridStyle}" IsReadOnly="True" AutoGenerateColumns="False">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Name" Binding="{Binding FullName}" Width="180"/>
                            <DataGridTextColumn Header="Username" Binding="{Binding Username}" Width="140"/>
                            <DataGridTextColumn Header="Department" Binding="{Binding Department}" Width="140"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </TabItem>
            </TabControl>
        </Grid>

        <!-- Right Panel: Staff Form -->
        <Border Grid.Column="1" Background="{StaticResource BackgroundWhite}" CornerRadius="8" BorderBrush="{StaticResource BorderColor}" BorderThickness="1" Padding="16">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <TextBlock FontSize="18" FontWeight="SemiBold" Foreground="{StaticResource TextPrimary}" Margin="0,0,0,12">
                        <Run Text="{Binding IsEditing, StringFormat='Edit Staff'}"/>
                        <Run Text="Staff"/>
                    </TextBlock>

                    <TextBlock Text="Account Information" FontSize="13" FontWeight="SemiBold" Foreground="{StaticResource PrimaryColor}" Margin="0,0,0,6"/>
                    <TextBlock Text="First Name *" FontSize="11" FontWeight="Medium" Foreground="{StaticResource TextSecondary}"/>
                    <TextBox Text="{Binding FirstName, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource InputStyle}" Padding="8,6" Margin="0,0,0,8"/>

                    <TextBlock Text="Last Name *" FontSize="11" FontWeight="Medium" Foreground="{StaticResource TextSecondary}"/>
                    <TextBox Text="{Binding LastName, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource InputStyle}" Padding="8,6" Margin="0,0,0,8"/>

                    <TextBlock Text="Username" FontSize="11" FontWeight="Medium" Foreground="{StaticResource TextSecondary}"/>
                    <TextBox Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource InputStyle}" Padding="8,6" Margin="0,0,0,8"/>

                    <TextBlock Text="Password (blank to keep current)" FontSize="11" FontWeight="Medium" Foreground="{StaticResource TextSecondary}"/>
                    <TextBox Text="{Binding Password, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource InputStyle}" Padding="8,6" Margin="0,0,0,8"/>

                    <TextBlock Text="Email" FontSize="11" FontWeight="Medium" Foreground="{StaticResource TextSecondary}"/>
                    <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource InputStyle}" Padding="8,6" Margin="0,0,0,8"/>

                    <TextBlock Text="Phone" FontSize="11" FontWeight="Medium" Foreground="{StaticResource TextSecondary}"/>
                    <TextBox Text="{Binding Phone, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource InputStyle}" Padding="8,6" Margin="0,0,0,8"/>

                    <TextBlock Text="Role" FontSize="13" FontWeight="SemiBold" Foreground="{StaticResource PrimaryColor}" Margin="0,8,0,6"/>
                    <ComboBox SelectedItem="{Binding SelectedRole, UpdateSourceTrigger=PropertyChanged}" Padding="8,6" Margin="0,0,0,8">
                        <ComboBoxItem Content="Administrator"/><ComboBoxItem Content="Physician"/>
                        <ComboBoxItem Content="Nurse"/><ComboBoxItem Content="HardwareTechnician"/>
                        <ComboBoxItem Content="ReportWriter"/><ComboBoxItem Content="BiomedicalEngineer"/>
                        <ComboBoxItem Content="LabTechnician"/><ComboBoxItem Content="Radiologist"/>
                        <ComboBoxItem Content="ITSupport"/><ComboBoxItem Content="Staff"/>
                    </ComboBox>

                    <TextBlock Text="Department" FontSize="11" FontWeight="Medium" Foreground="{StaticResource TextSecondary}"/>
                    <TextBox Text="{Binding Department, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource InputStyle}" Padding="8,6" Margin="0,0,0,8"/>

                    <TextBlock Text="Job Title" FontSize="11" FontWeight="Medium" Foreground="{StaticResource TextSecondary}"/>
                    <TextBox Text="{Binding JobTitle, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource InputStyle}" Padding="8,6" Margin="0,0,0,8"/>

                    <TextBlock Text="Specialization" FontSize="11" FontWeight="Medium" Foreground="{StaticResource TextSecondary}"/>
                    <TextBox Text="{Binding Specialization, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource InputStyle}" Padding="8,6" Margin="0,0,0,8"/>

                    <TextBlock Text="Permissions" FontSize="13" FontWeight="SemiBold" Foreground="{StaticResource PrimaryColor}" Margin="0,8,0,6"/>
                    <CheckBox Content="Manage Devices" IsChecked="{Binding CanManageDevices}" Margin="0,4"/>
                    <CheckBox Content="Manage Maintenance" IsChecked="{Binding CanManageMaintenance}" Margin="0,4"/>
                    <CheckBox Content="Manage Calibration" IsChecked="{Binding CanManageCalibration}" Margin="0,4"/>
                    <CheckBox Content="View Reports" IsChecked="{Binding CanViewReports}" Margin="0,4"/>
                    <CheckBox Content="Access Admin Panel" IsChecked="{Binding CanAccessAdminPanel}" Margin="0,4"/>

                    <StackPanel Orientation="Horizontal" Margin="0,16,0,0">
                        <Button Content="➕ New" Command="{Binding StartAddNewCommand}" Style="{StaticResource SecondaryButtonStyle}" Width="80" Margin="0,0,8,0"/>
                        <Button Content="✏️ Edit" Command="{Binding StartEditCommand}" CommandParameter="{Binding SelectedStaff}" Style="{StaticResource SecondaryButtonStyle}" Width="80" Margin="0,0,8,0"/>
                        <Button Content="🔑 Reset Password" Command="{Binding ResetPasswordCommand}" Style="{StaticResource SecondaryButtonStyle}" Width="120" Margin="0,0,8,0"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                        <Button Content="💾 Save" Command="{Binding SaveStaffCommand}" Style="{StaticResource PrimaryButtonStyle}" Width="100" Margin="0,0,8,0"/>
                        <Button Content="🗑️ Delete" Command="{Binding DeleteStaffCommand}" Style="{StaticResource SecondaryButtonStyle}" Width="80" Margin="0,0,8,0"/>
                        <Button Content="🔒 Lock/Unlock" Command="{Binding ToggleLockCommand}" Style="{StaticResource SecondaryButtonStyle}" Width="110"/>
                    </StackPanel>

                    <TextBlock Text="{Binding StatusMessage}" Foreground="{StaticResource TextSecondary}" FontSize="11" Margin="0,12,0,0" TextWrapping="Wrap"/>
                </StackPanel>
            </ScrollViewer>
        </Border>
    </Grid>
</UserControl>
