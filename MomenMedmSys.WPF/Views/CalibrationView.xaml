<UserControl x:Class="MomenMedmSys.WPF.Views.CalibrationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignHeight="900" d:DesignWidth="1400">
    <Grid Background="#F1F5F9">
        <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>

        <!-- Top Header Bar -->
        <Border Grid.Row="0" Background="White" BorderBrush="#E2E8F0" BorderThickness="0,0,0,1" Padding="20,14">
            <Grid>
                <Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <Border Background="#8B5CF6" Width="40" Height="40" Margin="0,0,14,0"><TextBlock Text="📏" FontSize="20" HorizontalAlignment="Center" VerticalAlignment="Center"/></Border>
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="Calibration" FontSize="22" FontWeight="Bold" Foreground="#0F172A"/>
                        <TextBlock Text="Manage equipment calibration records" FontSize="12" Foreground="#94A3B8" Margin="0,2,0,0"/>
                    </StackPanel>
                </StackPanel>
                <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                    <Border Background="#F1F5F9" Padding="12,9" Width="240" Margin="0,0,12,0">
                        <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                            <TextBlock Text="🔍" FontSize="14" Grid.Column="0" VerticalAlignment="Center" Margin="0,0,8,0"/>
                            <TextBox Grid.Column="1" Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" Background="Transparent" BorderThickness="0" FontSize="13" Padding="0" VerticalAlignment="Center"/>
                        </Grid>
                    </Border>
                    <Button Content="🔄 Refresh" Command="{Binding LoadRecordsCommand}" Style="{StaticResource PrimaryBtn}" Padding="14,9"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Main Content -->
        <Grid Grid.Row="1" Margin="16,14">
            <Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/></Grid.RowDefinitions>

            <!-- Stats Cards -->
            <WrapPanel Grid.Row="0" Margin="0,0,0,12">
                <Border Style="{StaticResource StatCardTeal}">
                    <StackPanel><TextBlock Text="📏 Total Records" FontSize="10" Foreground="White" Opacity="0.9"/><TextBlock Text="{Binding Records.Count}" FontSize="28" FontWeight="Bold" Foreground="White" Margin="0,4,0,0"/></StackPanel>
                </Border>
                <Border Style="{StaticResource StatCardGreen}">
                    <StackPanel><TextBlock Text="✅ Pass" FontSize="10" Foreground="White" Opacity="0.9"/><TextBlock Text="{Binding PassCount}" FontSize="28" FontWeight="Bold" Foreground="White" Margin="0,4,0,0"/></StackPanel>
                </Border>
                <Border Style="{StaticResource StatCardRed}">
                    <StackPanel><TextBlock Text="❌ Fail" FontSize="10" Foreground="White" Opacity="0.9"/><TextBlock Text="{Binding FailCount}" FontSize="28" FontWeight="Bold" Foreground="White" Margin="0,4,0,0"/></StackPanel>
                </Border>
                <Border Style="{StaticResource StatCardAmber}">
                    <StackPanel><TextBlock Text="⏰ Overdue" FontSize="10" Foreground="White" Opacity="0.9"/><TextBlock Text="{Binding OverdueCount}" FontSize="28" FontWeight="Bold" Foreground="White" Margin="0,4,0,0"/></StackPanel>
                </Border>
            </WrapPanel>

            <!-- Data Grid Card -->
            <Border Grid.Row="1" Style="{StaticResource GlassCard}" Padding="0">
                <Grid><Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
                    <!-- Toolbar -->
                    <Border Grid.Row="0" Background="#F8FAFC" Padding="16,12" BorderBrush="#E2E8F0" BorderThickness="0,0,0,1">
                        <WrapPanel>
                            <TextBlock Text="⚡ Actions:" FontSize="12" FontWeight="SemiBold" Foreground="#475569" VerticalAlignment="Center" Margin="0,0,12,0"/>
                            <Button Content="➕ Add Record" Command="{Binding AddRecordCommand}" Style="{StaticResource PrimaryBtn}" Padding="14,9" Margin="0,0,8,0"/>
                            <Button Content="✏️ Edit" Command="{Binding EditRecordCommand}" CommandParameter="{Binding SelectedRecord}" Style="{StaticResource SecondaryBtn}" Padding="14,9" Margin="0,0,8,0"/>
                            <TextBlock Text="Result:" FontSize="12" FontWeight="SemiBold" Foreground="#475569" VerticalAlignment="Center" Margin="0,0,8,0"/>
                            <ComboBox Text="{Binding FilterResult, UpdateSourceTrigger=PropertyChanged}" Width="140" Style="{StaticResource ModernCombo}">
                                <ComboBoxItem Content="All Results"/><ComboBoxItem Content="Pass"/><ComboBoxItem Content="PassWithAdjustment"/><ComboBoxItem Content="Fail"/><ComboBoxItem Content="OutOfTolerance"/>
                            </ComboBox>
                        </WrapPanel>
                    </Border>

                    <!-- DataGrid -->
                    <DataGrid Grid.Row="1" ItemsSource="{Binding FilteredRecords}" SelectedItem="{Binding SelectedRecord}" Style="{StaticResource ModernDataGrid}">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="TYPE" Binding="{Binding CalibrationType}" Width="120">
                                <DataGridTextColumn.ElementStyle><Style TargetType="TextBlock"><Setter Property="FontWeight" Value="SemiBold"/></Style></DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn Header="DEVICE" Binding="{Binding DeviceName}" Width="*">
                                <DataGridTextColumn.ElementStyle><Style TargetType="TextBlock"><Setter Property="FontWeight" Value="SemiBold"/></Style></DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>
                            <DataGridTextColumn Header="DATE" Binding="{Binding CalibrationDate, StringFormat=yyyy-MM-dd}" Width="100"/>
                            <DataGridTextColumn Header="NEXT DUE" Binding="{Binding NextDueDate, StringFormat=yyyy-MM-dd}" Width="100"/>
                            <DataGridTextColumn Header="PERFORMED BY" Binding="{Binding PerformedBy}" Width="130"/>
                            <DataGridTextColumn Header="CERTIFICATE" Binding="{Binding CertificateNumber}" Width="120"/>
                            <DataGridTemplateColumn Header="RESULT" Width="130">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Border CornerRadius="6" Padding="8,4" HorizontalAlignment="Center" Width="115">
                                            <TextBlock FontSize="10" FontWeight="SemiBold" HorizontalAlignment="Center">
                                                <TextBlock.Style><Style TargetType="TextBlock"><Style.Triggers>
                                                    <DataTrigger Binding="{Binding Result}" Value="Pass"><Setter Property="Text" Value="✅ Pass"/><Setter Property="Foreground" Value="#059669"/></DataTrigger>
                                                    <DataTrigger Binding="{Binding Result}" Value="PassWithAdjustment"><Setter Property="Text" Value="🟡 Pass+Adjust"/><Setter Property="Foreground" Value="#D97706"/></DataTrigger>
                                                    <DataTrigger Binding="{Binding Result}" Value="Fail"><Setter Property="Text" Value="❌ Fail"/><Setter Property="Foreground" Value="#DC2626"/></DataTrigger>
                                                    <DataTrigger Binding="{Binding Result}" Value="OutOfTolerance"><Setter Property="Text" Value="⚠️ Out of Tol"/><Setter Property="Foreground" Value="#991B1B"/></DataTrigger>
                                                </Style.Triggers></Style></TextBlock.Style>
                                            </TextBlock>
                                            <Border.Style><Style TargetType="Border"><Style.Triggers>
                                                <DataTrigger Binding="{Binding Result}" Value="Pass"><Setter Property="Background" Value="#D1FAE5"/></DataTrigger>
                                                <DataTrigger Binding="{Binding Result}" Value="PassWithAdjustment"><Setter Property="Background" Value="#FEF3C7"/></DataTrigger>
                                                <DataTrigger Binding="{Binding Result}" Value="Fail"><Setter Property="Background" Value="#FEE2E2"/></DataTrigger>
                                                <DataTrigger Binding="{Binding Result}" Value="OutOfTolerance"><Setter Property="Background" Value="#FECACA"/></DataTrigger>
                                            </Style.Triggers></Style></Border.Style>
                                        </Border>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTemplateColumn Header="LAB" Width="80">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <TextBlock HorizontalAlignment="Center" FontSize="11">
                                            <TextBlock.Style><Style TargetType="TextBlock"><Style.Triggers>
                                                <DataTrigger Binding="{Binding IsExternalLab}" Value="True"><Setter Property="Text" Value="🏢 External"/><Setter Property="Foreground" Value="#2563EB"/></DataTrigger>
                                                <DataTrigger Binding="{Binding IsExternalLab}" Value="False"><Setter Property="Text" Value="🏠 Internal"/><Setter Property="Foreground" Value="#059669"/></DataTrigger>
                                            </Style.Triggers></Style></TextBlock.Style>
                                        </TextBlock>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </Border>

            <!-- Status Bar -->
            <Border Grid.Row="2" Background="White" Padding="14,10" Margin="0,12,0,0" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="10">
                <Grid><Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="{Binding StatusMessage}" Foreground="#475569" FontSize="12"/>
                    <TextBlock Grid.Column="1" Foreground="#64748B" FontSize="12">
                        <TextBlock.Text><MultiBinding StringFormat="Showing {0} of {1} records"><Binding Path="FilteredRecords.Count"/><Binding Path="Records.Count"/></MultiBinding></TextBlock.Text>
                    </TextBlock>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>
