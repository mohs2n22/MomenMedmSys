<UserControl x:Class="MomenMedmSys.WPF.Views.CalibrationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="📏 Calibration" FontSize="28" FontWeight="Bold" Foreground="{StaticResource TextPrimary}"/>
            <TextBlock Text="Manage equipment calibration records" FontSize="14" Foreground="{StaticResource TextSecondary}"/>
        </StackPanel>
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,16">
            <Border Style="{StaticResource StatCardSuccess}" Margin="0,0,10,0" MinWidth="100">
                <StackPanel Orientation="Horizontal"><TextBlock Text="✅ Pass: " Foreground="{StaticResource TextOnPrimary}" FontSize="12"/><TextBlock Text="{Binding PassCount}" Foreground="{StaticResource TextOnPrimary}" FontSize="14" FontWeight="Bold"/></StackPanel>
            </Border>
            <Border Style="{StaticResource StatCardDanger}" Margin="0,0,10,0" MinWidth="100">
                <StackPanel Orientation="Horizontal"><TextBlock Text="❌ Fail: " Foreground="{StaticResource TextOnPrimary}" FontSize="12"/><TextBlock Text="{Binding FailCount}" Foreground="{StaticResource TextOnPrimary}" FontSize="14" FontWeight="Bold"/></StackPanel>
            </Border>
            <Border Style="{StaticResource StatCardWarning}" Margin="0,0,10,0" MinWidth="100">
                <StackPanel Orientation="Horizontal"><TextBlock Text="⏰ Overdue: " Foreground="{StaticResource TextOnPrimary}" FontSize="12"/><TextBlock Text="{Binding OverdueCount}" Foreground="{StaticResource TextOnPrimary}" FontSize="14" FontWeight="Bold"/></StackPanel>
            </Border>
            <Button Content="➕ Add" Command="{Binding AddRecordCommand}" Style="{StaticResource PrimaryButtonStyle}" Width="80" Margin="16,0,8,0"/>
            <Button Content="✏️ Edit" Command="{Binding EditRecordCommand}" Style="{StaticResource SecondaryButtonStyle}" Width="80" Margin="0,0,8,0"/>
            <Button Content="🔄 Refresh" Command="{Binding LoadRecordsCommand}" Style="{StaticResource SecondaryButtonStyle}" Width="100"/>
        </StackPanel>
        <DataGrid Grid.Row="2" ItemsSource="{Binding Records}" SelectedItem="{Binding SelectedRecord}"
                  Style="{StaticResource DataGridStyle}" IsReadOnly="True" AutoGenerateColumns="False"
                  SelectionMode="Single" CanUserSortColumns="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Type" Binding="{Binding CalibrationType}" Width="120"/>
                <DataGridTextColumn Header="Device" Binding="{Binding Device.DeviceName}" Width="180"/>
                <DataGridTextColumn Header="Date" Binding="{Binding CalibrationDate, StringFormat=yyyy-MM-dd}" Width="100"/>
                <DataGridTextColumn Header="Next Due" Binding="{Binding NextDueDate, StringFormat=yyyy-MM-dd}" Width="100"/>
                <DataGridTextColumn Header="Performed By" Binding="{Binding PerformedBy}" Width="120"/>
                <DataGridTemplateColumn Header="Result" Width="100">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Border CornerRadius="4" Padding="6,2" HorizontalAlignment="Center">
                                <TextBlock Text="{Binding Result}" FontSize="11" FontWeight="SemiBold" HorizontalAlignment="Center"/>
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Result}" Value="Pass">
                                                <Setter Property="Background" Value="{StaticResource SuccessBgColor}"/>
                                                <Setter Property="TextElement.Foreground" Value="{StaticResource TextOnSuccess}"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Result}" Value="PassWithAdjustment">
                                                <Setter Property="Background" Value="{StaticResource WarningBgColor}"/>
                                                <Setter Property="TextElement.Foreground" Value="{StaticResource TextOnWarning}"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Result}" Value="Fail">
                                                <Setter Property="Background" Value="{StaticResource DangerBgColor}"/>
                                                <Setter Property="TextElement.Foreground" Value="{StaticResource TextOnDanger}"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Result}" Value="OutOfTolerance">
                                                <Setter Property="Background" Value="{StaticResource DangerBgColor}"/>
                                                <Setter Property="TextElement.Foreground" Value="{StaticResource TextOnDanger}"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                            </Border>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="Certificate" Binding="{Binding CertificateNumber}" Width="120"/>
                <DataGridTextColumn Header="External Lab" Binding="{Binding IsExternalLab}" Width="90"/>
            </DataGrid.Columns>
        </DataGrid>
        <TextBlock Grid.Row="3" Text="{Binding StatusMessage}" Foreground="{StaticResource TextSecondary}" FontSize="12" Margin="0,8,0,0"/>
    </Grid>
</UserControl>
