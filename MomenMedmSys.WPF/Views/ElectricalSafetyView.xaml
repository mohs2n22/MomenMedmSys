<UserControl x:Class="MomenMedmSys.WPF.Views.ElectricalSafetyView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="Electrical Safety Tests" FontSize="28" FontWeight="Bold" Foreground="#1E293B"/>
            <TextBlock Text="IEC 60601-1 electrical safety testing for patient and staff protection" FontSize="14" Foreground="#64748B"/>
        </StackPanel>
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,16">
            <Border Background="#22C55E" CornerRadius="6" Padding="16,8" Margin="0,0,12,0">
                <StackPanel Orientation="Horizontal"><TextBlock Text="✅ Pass: " Foreground="White" FontSize="12"/><TextBlock Text="{Binding PassCount}" Foreground="White" FontSize="14" FontWeight="Bold"/></StackPanel>
            </Border>
            <Border Background="#EF4444" CornerRadius="6" Padding="16,8" Margin="0,0,12,0">
                <StackPanel Orientation="Horizontal"><TextBlock Text="❌ Fail: " Foreground="White" FontSize="12"/><TextBlock Text="{Binding FailCount}" Foreground="White" FontSize="14" FontWeight="Bold"/></StackPanel>
            </Border>
            <Border Background="#F59E0B" CornerRadius="6" Padding="16,8" Margin="0,0,12,0">
                <StackPanel Orientation="Horizontal"><TextBlock Text="⏰ Overdue: " Foreground="White" FontSize="12"/><TextBlock Text="{Binding OverdueCount}" Foreground="White" FontSize="14" FontWeight="Bold"/></StackPanel>
            </Border>
            <Button Content="🔄 Refresh" Command="{Binding LoadTestsCommand}" Style="{StaticResource PrimaryBtn}" Width="100" Margin="16,0,8,0"/>
            <Button Content="🗑️ Delete" Command="{Binding DeleteTestCommand}" Style="{StaticResource SecondaryBtn}" Width="90"/>
        </StackPanel>
        <DataGrid Grid.Row="2" ItemsSource="{Binding Tests}" SelectedItem="{Binding SelectedTest}"
                  Style="{StaticResource ModernDataGrid}" IsReadOnly="True" AutoGenerateColumns="False"
                  SelectionMode="Single" CanUserSortColumns="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Device" Binding="{Binding Device.DeviceName}" Width="160"/>
                <DataGridTextColumn Header="Test Date" Binding="{Binding TestDate, StringFormat=yyyy-MM-dd}" Width="100"/>
                <DataGridTextColumn Header="Next Due" Binding="{Binding NextDueDate, StringFormat=yyyy-MM-dd}" Width="100"/>
                <DataGridTextColumn Header="Test Type" Binding="{Binding TestType}" Width="120"/>
                <DataGridTextColumn Header="Standard" Binding="{Binding TestStandard}" Width="120"/>
                <DataGridTextColumn Header="Earth (Ω)" Binding="{Binding EarthResistanceMeasured}" Width="80"/>
                <DataGridTextColumn Header="Leakage (mA)" Binding="{Binding LeakageCurrentMeasured}" Width="100"/>
                <DataGridTextColumn Header="Insulation (MΩ)" Binding="{Binding InsulationResistanceMeasured}" Width="110"/>
                <DataGridTextColumn Header="Visual" Binding="{Binding VisualInspectionPass}" Width="70"/>
                <DataGridTextColumn Header="Result" Binding="{Binding OverallResult}" Width="100"/>
                <DataGridTextColumn Header="Performed By" Binding="{Binding PerformedBy}" Width="120"/>
                <DataGridTextColumn Header="Certificate" Binding="{Binding CertificateNumber}" Width="120"/>
            </DataGrid.Columns>
        </DataGrid>
        <TextBlock Grid.Row="3" Text="{Binding StatusMessage}" Foreground="#64748B" FontSize="12" Margin="0,8,0,0"/>
    </Grid>
</UserControl>

