<UserControl x:Class="MomenMedmSys.WPF.Views.SuppliersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="Suppliers" FontSize="28" FontWeight="Bold" Foreground="#1E293B"/>
            <TextBlock Text="Manage equipment and parts suppliers" FontSize="14" Foreground="#64748B"/>
        </StackPanel>

        <!-- Toolbar -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,16">
            <Button Content="➕ Add" Command="{Binding AddSupplierCommand}" Style="{StaticResource PrimaryBtn}" Width="90" Margin="0,0,8,0"/>
            <Button Content="✏️ Edit" Command="{Binding EditSupplierCommand}" Style="{StaticResource SecondaryBtn}" Width="90" Margin="0,0,8,0"/>
            <Button Content="🗑️ Delete" Command="{Binding DeleteSupplierCommand}" Style="{StaticResource SecondaryBtn}" Width="90" Margin="0,0,8,0"/>
            <Button Content="🔄 Refresh" Command="{Binding RefreshCommand}" Style="{StaticResource SecondaryBtn}" Width="100"/>
        </StackPanel>

        <!-- DataGrid -->
        <DataGrid Grid.Row="2" ItemsSource="{Binding Suppliers}" 
                  Style="{StaticResource ModernDataGrid}" IsReadOnly="True" AutoGenerateColumns="False"
                  SelectionMode="Single" CanUserSortColumns="True" CanUserResizeColumns="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Company Name" Binding="{Binding CompanyName}" Width="200"/>
                <DataGridTextColumn Header="Contact Person" Binding="{Binding ContactPerson}" Width="150"/>
                <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="180"/>
                <DataGridTextColumn Header="Phone" Binding="{Binding Phone}" Width="120"/>
                <DataGridTextColumn Header="Country" Binding="{Binding Country}" Width="100"/>
                <DataGridTextColumn Header="Rating" Binding="{Binding Rating}" Width="80"/>
                <DataGridTemplateColumn Header="Approved" Width="90">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding IsApproved}" HorizontalAlignment="Center" VerticalAlignment="Center">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsApproved}" Value="True">
                                                <Setter Property="Foreground" Value="#166534"/>
                                                <Setter Property="Text" Value="✓"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding IsApproved}" Value="False">
                                                <Setter Property="Foreground" Value="#991B1B"/>
                                                <Setter Property="Text" Value="✗"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Status Bar -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,12,0,0">
            <TextBlock Text="{Binding StatusMessage}" Foreground="#64748B" FontSize="12"/>
            <TextBlock Text=" | " Foreground="#CBD5E1" FontSize="12"/>
            <TextBlock Foreground="#64748B" FontSize="12">
                <TextBlock.Text>
                    <MultiBinding StringFormat="Total: {0} | Approved: {1}">
                        <Binding Path="TotalCount"/>
                        <Binding Path="ApprovedCount"/>
                    </MultiBinding>
                </TextBlock.Text>
            </TextBlock>
        </StackPanel>
    </Grid>
</UserControl>

