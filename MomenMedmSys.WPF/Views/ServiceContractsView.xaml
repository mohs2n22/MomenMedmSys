<UserControl x:Class="MomenMedmSys.WPF.Views.ServiceContractsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="📄 Service Contracts" FontSize="28" FontWeight="Bold" Foreground="{StaticResource TextPrimary}"/>
            <TextBlock Text="Manage service contracts and agreements" FontSize="14" Foreground="{StaticResource TextSecondary}"/>
        </StackPanel>

        <!-- Stats Row -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,16">
            <Border Style="{StaticResource StatCard.Primary}" Margin="0,0,8,0" MinWidth="90">
                <StackPanel><TextBlock Text="📋 Total Contracts" FontSize="10" Foreground="White" Opacity="0.9"/><TextBlock Text="{Binding TotalCount}" FontSize="18" FontWeight="Bold" Foreground="White" Margin="0,2,0,0"/></StackPanel>
            </Border>
            <Border Style="{StaticResource StatCard.Success}" Margin="0,0,8,0" MinWidth="90">
                <StackPanel><TextBlock Text="✅ Active Status" FontSize="10" Foreground="White" Opacity="0.9"/><TextBlock Text="{Binding ActiveCount}" FontSize="18" FontWeight="Bold" Foreground="White" Margin="0,2,0,0"/></StackPanel>
            </Border>
            <Border Style="{StaticResource StatCard.Warning}" Margin="0,0,8,0" MinWidth="90">
                <StackPanel><TextBlock Text="⚠️ Expiring Soon" FontSize="10" Foreground="White" Opacity="0.9"/><TextBlock Text="{Binding ExpiringCount}" FontSize="18" FontWeight="Bold" Foreground="White" Margin="0,2,0,0"/></StackPanel>
            </Border>
            <Button Content="➕ Add" Command="{Binding AddContractCommand}" Style="{StaticResource PrimaryBtn}" Width="80" Margin="16,0,8,0"/>
            <Button Content="✏️ Edit" Command="{Binding EditContractCommand}" CommandParameter="{Binding SelectedItem, ElementName=ContractsGrid}" Style="{StaticResource SecondaryBtn}" Width="80" Margin="0,0,8,0"/>
            <Button Content="🗑️ Delete" Command="{Binding DeleteContractCommand}" CommandParameter="{Binding SelectedItem, ElementName=ContractsGrid}" Style="{StaticResource SecondaryBtn}" Width="100" Margin="0,0,8,0"/>
            <Button Content="🔄 Refresh" Command="{Binding RefreshCommand}" Style="{StaticResource SecondaryBtn}" Width="100"/>
        </StackPanel>

        <!-- DataGrid -->
        <DataGrid x:Name="ContractsGrid" Grid.Row="2" ItemsSource="{Binding Contracts}" SelectedItem="{Binding SelectedContract}"
                  Style="{StaticResource ModernDataGrid}" IsReadOnly="True" AutoGenerateColumns="False"
                  SelectionMode="Single" CanUserSortColumns="True" CanUserResizeColumns="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Contract Number" Binding="{Binding ContractNumber}" Width="140"/>
                <DataGridTextColumn Header="Contract Name" Binding="{Binding ContractName}" Width="180"/>
                <DataGridTextColumn Header="Provider" Binding="{Binding Provider}" Width="160"/>
                <DataGridTextColumn Header="Start Date" Binding="{Binding StartDate, StringFormat=yyyy-MM-dd}" Width="100"/>
                <DataGridTextColumn Header="End Date" Binding="{Binding EndDate, StringFormat=yyyy-MM-dd}" Width="100"/>
                <DataGridTextColumn Header="Contract Value" Binding="{Binding ContractValue, StringFormat=C2}" Width="120"/>
                <DataGridTemplateColumn Header="Status" Width="100">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Border CornerRadius="4" Padding="6,2" HorizontalAlignment="Center">
                                <TextBlock Text="{Binding Status}" FontSize="11" FontWeight="SemiBold" HorizontalAlignment="Center"/>
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Status}" Value="Active">
                                                <Setter Property="Background" Value="{StaticResource SuccessBgColor}"/>
                                                <Setter Property="TextElement.Foreground" Value="{StaticResource TextOnSuccess}"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Status}" Value="Pending">
                                                <Setter Property="Background" Value="{StaticResource PrimaryBgColor}"/>
                                                <Setter Property="TextElement.Foreground" Value="{StaticResource PrimaryColor}"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Status}" Value="Expiring">
                                                <Setter Property="Background" Value="{StaticResource WarningBgColor}"/>
                                                <Setter Property="TextElement.Foreground" Value="{StaticResource TextOnWarning}"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Status}" Value="Expired">
                                                <Setter Property="Background" Value="{StaticResource DangerBgColor}"/>
                                                <Setter Property="TextElement.Foreground" Value="{StaticResource TextOnDanger}"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Status}" Value="Cancelled">
                                                <Setter Property="Background" Value="{StaticResource SecondaryBgColor}"/>
                                                <Setter Property="TextElement.Foreground" Value="{StaticResource SecondaryColor}"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                            </Border>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Status Bar -->
        <TextBlock Grid.Row="3" Text="{Binding StatusMessage}" Foreground="{StaticResource TextSecondary}" FontSize="12" Margin="0,8,0,0"/>
    </Grid>
</UserControl>

