<UserControl x:Class="MomenMedmSys.WPF.Views.ReportsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="Reports" FontSize="28" FontWeight="Bold" Foreground="#1E293B"/>
            <TextBlock Text="System analytics and summary reports" FontSize="14" Foreground="#64748B"/>
        </StackPanel>
        <Button Grid.Row="1" Content="📊 Generate Report" Command="{Binding LoadReportCommand}" Style="{StaticResource PrimaryButtonStyle}" Width="160" HorizontalAlignment="Left" Margin="0,0,0,16"/>
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <Border Background="White" CornerRadius="8" Padding="24" BorderBrush="#E2E8F0" BorderThickness="1">
                <TextBlock Text="{Binding ReportSummary}" FontSize="14" FontFamily="Consolas" Foreground="#1E293B" TextWrapping="Wrap"/>
            </Border>
        </ScrollViewer>
    </Grid>
</UserControl>
