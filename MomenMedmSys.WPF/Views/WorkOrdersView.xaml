<UserControl x:Class="MomenMedmSys.WPF.Views.WorkOrdersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="Work Orders" FontSize="28" FontWeight="Bold" Foreground="#1E293B"/>
            <TextBlock Text="Track fault reports and repair requests" FontSize="14" Foreground="#64748B"/>
        </StackPanel>
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,16">
            <Border Background="#3B82F6" CornerRadius="6" Padding="16,8" Margin="0,0,12,0">
                <StackPanel Orientation="Horizontal"><TextBlock Text="Open: " Foreground="White" FontSize="12"/><TextBlock Text="{Binding OpenCount}" Foreground="White" FontSize="14" FontWeight="Bold"/></StackPanel>
            </Border>
            <Border Background="#EF4444" CornerRadius="6" Padding="16,8" Margin="0,0,12,0">
                <StackPanel Orientation="Horizontal"><TextBlock Text="Overdue: " Foreground="White" FontSize="12"/><TextBlock Text="{Binding OverdueCount}" Foreground="White" FontSize="14" FontWeight="Bold"/></StackPanel>
            </Border>
            <Button Content="🔄 Refresh" Command="{Binding LoadWorkOrdersCommand}" Style="{StaticResource PrimaryButtonStyle}" Width="100" Margin="16,0,8,0"/>
            <Button Content="🗑️ Delete" Command="{Binding DeleteWorkOrderCommand}" Style="{StaticResource SecondaryButtonStyle}" Width="90"/>
        </StackPanel>
        <DataGrid Grid.Row="2" ItemsSource="{Binding WorkOrders}" SelectedItem="{Binding SelectedWorkOrder}"
                  Style="{StaticResource DataGridStyle}" IsReadOnly="True" AutoGenerateColumns="False"
                  SelectionMode="Single" CanUserSortColumns="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="WO Number" Binding="{Binding WorkOrderNumber}" Width="130"/>
                <DataGridTextColumn Header="Device" Binding="{Binding Device.DeviceName}" Width="160"/>
                <DataGridTextColumn Header="Reported By" Binding="{Binding ReportedBy}" Width="120"/>
                <DataGridTextColumn Header="Report Date" Binding="{Binding ReportDate, StringFormat=yyyy-MM-dd}" Width="100"/>
                <DataGridTextColumn Header="Fault" Binding="{Binding FaultDescription}" Width="250"/>
                <DataGridTextColumn Header="Priority" Binding="{Binding Priority}" Width="80"/>
                <DataGridTextColumn Header="Assigned To" Binding="{Binding AssignedTo}" Width="120"/>
                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="100"/>
                <DataGridTextColumn Header="Est. Cost" Binding="{Binding EstimatedCost, StringFormat=C2}" Width="90"/>
                <DataGridTextColumn Header="Actual Cost" Binding="{Binding ActualCost, StringFormat=C2}" Width="90"/>
                <DataGridTextColumn Header="Response (hrs)" Binding="{Binding ResponseTimeHours}" Width="100"/>
                <DataGridTextColumn Header="Resolution (hrs)" Binding="{Binding ResolutionTimeHours}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>
        <TextBlock Grid.Row="3" Text="{Binding StatusMessage}" Foreground="#64748B" FontSize="12" Margin="0,8,0,0"/>
    </Grid>
</UserControl>
