<Application x:Class="MomenMedmSys.WPF.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:MomenMedmSys.WPF">
    <Application.Resources>
        <ResourceDictionary>
            <!-- ========================================== -->
            <!-- UNIFIED COLOR PALETTE - Medical Blue Theme -->
            <!-- ========================================== -->

            <!-- Primary Brand Colors -->
            <SolidColorBrush x:Key="PrimaryColor" Color="#1976D2"/>
            <SolidColorBrush x:Key="PrimaryDarkColor" Color="#1565C0"/>
            <SolidColorBrush x:Key="PrimaryLightColor" Color="#42A5F5"/>
            <SolidColorBrush x:Key="PrimaryLighterColor" Color="#90CAF9"/>
            <SolidColorBrush x:Key="PrimaryBgColor" Color="#E3F2FD"/>

            <!-- Status Colors -->
            <SolidColorBrush x:Key="SuccessColor" Color="#2E7D32"/>
            <SolidColorBrush x:Key="SuccessBgColor" Color="#E8F5E9"/>
            <SolidColorBrush x:Key="WarningColor" Color="#F57C00"/>
            <SolidColorBrush x:Key="WarningBgColor" Color="#FFF3E0"/>
            <SolidColorBrush x:Key="DangerColor" Color="#D32F2F"/>
            <SolidColorBrush x:Key="DangerBgColor" Color="#FFEBEE"/>
            <SolidColorBrush x:Key="InfoColor" Color="#1976D2"/>
            <SolidColorBrush x:Key="InfoBgColor" Color="#E3F2FD"/>
            <SolidColorBrush x:Key="CriticalColor" Color="#C62828"/>
            <SolidColorBrush x:Key="CriticalBgColor" Color="#FFCDD2"/>

            <!-- Accent Colors -->
            <SolidColorBrush x:Key="AccentColor" Color="#00ACC1"/>
            <SolidColorBrush x:Key="TealColor" Color="#00897B"/>
            <SolidColorBrush x:Key="TealBgColor" Color="#E0F2F1"/>
            <SolidColorBrush x:Key="PurpleColor" Color="#7B1FA2"/>
            <SolidColorBrush x:Key="PurpleBgColor" Color="#F3E5F5"/>
            <SolidColorBrush x:Key="IndigoColor" Color="#303F9F"/>
            <SolidColorBrush x:Key="IndigoBgColor" Color="#E8EAF6"/>

            <!-- Background Colors -->
            <SolidColorBrush x:Key="BackgroundDark" Color="#263238"/>
            <SolidColorBrush x:Key="BackgroundMedium" Color="#37474F"/>
            <SolidColorBrush x:Key="BackgroundLight" Color="#F5F7FA"/>
            <SolidColorBrush x:Key="BackgroundWhite" Color="#FFFFFF"/>
            <SolidColorBrush x:Key="SidebarColor" Color="#1E293B"/>
            <SolidColorBrush x:Key="SidebarHoverColor" Color="#334155"/>
            <SolidColorBrush x:Key="SidebarActiveColor" Color="#1976D2"/>

            <!-- Text Colors -->
            <SolidColorBrush x:Key="TextPrimary" Color="#212121"/>
            <SolidColorBrush x:Key="TextSecondary" Color="#64748B"/>
            <SolidColorBrush x:Key="TextMuted" Color="#94A3B8"/>
            <SolidColorBrush x:Key="TextOnPrimary" Color="#FFFFFF"/>
            <SolidColorBrush x:Key="TextOnDark" Color="#E2E8F0"/>
            <SolidColorBrush x:Key="TextLight" Color="#B0BEC5"/>
            <SolidColorBrush x:Key="TextOnSuccess" Color="#1B5E20"/>
            <SolidColorBrush x:Key="TextOnWarning" Color="#E65100"/>
            <SolidColorBrush x:Key="TextOnDanger" Color="#B71C1C"/>

            <!-- Border Colors -->
            <SolidColorBrush x:Key="BorderColor" Color="#E2E8F0"/>
            <SolidColorBrush x:Key="BorderLightColor" Color="#F1F5F9"/>
            <SolidColorBrush x:Key="BorderFocusColor" Color="#1976D2"/>

            <!-- Typography -->
            <FontFamily x:Key="DefaultFont">Segoe UI</FontFamily>

            <!-- ========================================== -->
            <!-- STAT CARD STYLES -->
            <!-- ========================================== -->

            <!-- Primary Blue Card -->
            <Style x:Key="StatCardPrimary" TargetType="Border">
                <Setter Property="Background" Value="{StaticResource PrimaryColor}"/>
                <Setter Property="CornerRadius" Value="8"/>
                <Setter Property="Padding" Value="16,12"/>
            </Style>

            <!-- Success Green Card -->
            <Style x:Key="StatCardSuccess" TargetType="Border">
                <Setter Property="Background" Value="{StaticResource SuccessColor}"/>
                <Setter Property="CornerRadius" Value="8"/>
                <Setter Property="Padding" Value="16,12"/>
            </Style>

            <!-- Warning Orange Card -->
            <Style x:Key="StatCardWarning" TargetType="Border">
                <Setter Property="Background" Value="{StaticResource WarningColor}"/>
                <Setter Property="CornerRadius" Value="8"/>
                <Setter Property="Padding" Value="16,12"/>
            </Style>

            <!-- Danger Red Card -->
            <Style x:Key="StatCardDanger" TargetType="Border">
                <Setter Property="Background" Value="{StaticResource DangerColor}"/>
                <Setter Property="CornerRadius" Value="8"/>
                <Setter Property="Padding" Value="16,12"/>
            </Style>

            <!-- Teal Card -->
            <Style x:Key="StatCardTeal" TargetType="Border">
                <Setter Property="Background" Value="{StaticResource TealColor}"/>
                <Setter Property="CornerRadius" Value="8"/>
                <Setter Property="Padding" Value="16,12"/>
            </Style>

            <!-- Purple Card -->
            <Style x:Key="StatCardPurple" TargetType="Border">
                <Setter Property="Background" Value="{StaticResource PurpleColor}"/>
                <Setter Property="CornerRadius" Value="8"/>
                <Setter Property="Padding" Value="16,12"/>
            </Style>

            <!-- Indigo Card -->
            <Style x:Key="StatCardIndigo" TargetType="Border">
                <Setter Property="Background" Value="{StaticResource IndigoColor}"/>
                <Setter Property="CornerRadius" Value="8"/>
                <Setter Property="Padding" Value="16,12"/>
            </Style>

            <!-- ========================================== -->
            <!-- BUTTON STYLES -->
            <!-- ========================================== -->

            <Style TargetType="Button" x:Key="PrimaryButtonStyle">
                <Setter Property="Background" Value="{StaticResource PrimaryColor}"/>
                <Setter Property="Foreground" Value="{StaticResource TextOnPrimary}"/>
                <Setter Property="Padding" Value="16,8"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="FontSize" Value="13"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="border" Background="{TemplateBinding Background}"
                                    CornerRadius="6" Padding="{TemplateBinding Padding}">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="border" Property="Background" Value="{StaticResource PrimaryDarkColor}"/>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter TargetName="border" Property="Background" Value="{StaticResource PrimaryLightColor}"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style TargetType="Button" x:Key="SecondaryButtonStyle">
                <Setter Property="Background" Value="{StaticResource BackgroundWhite}"/>
                <Setter Property="Foreground" Value="{StaticResource TextPrimary}"/>
                <Setter Property="Padding" Value="16,8"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="BorderBrush" Value="{StaticResource BorderColor}"/>
                <Setter Property="FontSize" Value="13"/>
                <Setter Property="FontWeight" Value="Medium"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="border" Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="6" Padding="{TemplateBinding Padding}">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="BorderBrush" Value="{StaticResource BorderFocusColor}"/>
                                    <Setter Property="Foreground" Value="{StaticResource PrimaryColor}"/>
                                    <Setter Property="Background" Value="{StaticResource PrimaryBgColor}"/>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter Property="Background" Value="{StaticResource BorderLightColor}"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- ========================================== -->
            <!-- INPUT STYLES -->
            <!-- ========================================== -->

            <Style TargetType="TextBox" x:Key="InputStyle">
                <Setter Property="Padding" Value="10,8"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="BorderBrush" Value="{StaticResource BorderColor}"/>
                <Setter Property="FontSize" Value="14"/>
                <Setter Property="FontFamily" Value="{StaticResource DefaultFont}"/>
            </Style>

            <!-- ========================================== -->
            <!-- DATAGRID STYLES -->
            <!-- ========================================== -->

            <Style TargetType="DataGrid" x:Key="DataGridStyle">
                <Setter Property="Background" Value="{StaticResource BackgroundWhite}"/>
                <Setter Property="BorderBrush" Value="{StaticResource BorderColor}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="RowBackground" Value="{StaticResource BackgroundWhite}"/>
                <Setter Property="AlternatingRowBackground" Value="#F8FAFC"/>
                <Setter Property="GridLinesVisibility" Value="Horizontal"/>
                <Setter Property="HorizontalGridLinesBrush" Value="{StaticResource BorderLightColor}"/>
                <Setter Property="FontSize" Value="13"/>
                <Setter Property="FontFamily" Value="{StaticResource DefaultFont}"/>
                <Setter Property="IsReadOnly" Value="True"/>
                <Setter Property="AutoGenerateColumns" Value="False"/>
                <Setter Property="HeadersVisibility" Value="Column"/>
                <Setter Property="ColumnHeaderStyle">
                    <Setter.Value>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="#F8FAFC"/>
                            <Setter Property="Foreground" Value="{StaticResource TextSecondary}"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                            <Setter Property="Padding" Value="12,8"/>
                            <Setter Property="BorderBrush" Value="{StaticResource BorderColor}"/>
                            <Setter Property="BorderThickness" Value="0,0,0,1"/>
                        </Style>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- ========================================== -->
            <!-- LOGO PATH DATA -->
            <!-- ========================================== -->
            <PathGeometry x:Key="LogoIconPath" Figures="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm-2 15l-5-5 1.41-1.41L10 14.17l7.59-7.59L19 8l-9 9z"/>
        </ResourceDictionary>
    </Application.Resources>
</Application>
